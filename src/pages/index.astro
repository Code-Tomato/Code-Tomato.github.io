---
import BaseLayout from '../layouts/BaseLayout.astro';
import ContactForm from '../components/ContactForm.astro';
import { getRecentProjects } from '../data/projects';

const recentProjects = getRecentProjects(3);
---

<BaseLayout title="Home" description="Nathan Lemma - ECE student at UT Austin specializing in systems programming, embedded systems, and hardware design">
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-background">
      <div class="gradient-orb orb-1"></div>
      <div class="gradient-orb orb-2"></div>
      <div class="gradient-orb orb-3"></div>
    </div>
    
    <div class="container hero-content">
      <div class="hero-text reveal">
        <div class="hero-badge">
          <span class="badge-dot"></span>
          Available for work
        </div>
        <h1 class="hero-title">
          Hi, I'm <span class="gradient-text">Nathan Lemma</span>
        </h1>
        <div class="typing-container">
          <p class="hero-subtitle">
            I explore <span class="typed-text"></span><span class="cursor">|</span>
          </p>
        </div>
        <p class="hero-description">
          An ECE student at UT Austin exploring systems programming, hardware design, 
          and embedded systems through research and industry work.
        </p>
        <div class="hero-cta">
          <a href="#contact" class="btn btn-primary">Get In Touch</a>
          <a href="/projects" class="btn btn-secondary">View Projects</a>
        </div>
      </div>
      
      <div class="hero-image reveal reveal-delay-1">
        <div class="floating-card card-1">
          <div class="card-icon">⚡</div>
          <div class="card-text">Systems Programming</div>
        </div>
        <div class="floating-card card-2">
          <div class="card-icon">🔧</div>
          <div class="card-text">Embedded Systems</div>
        </div>
        <div class="floating-card card-3">
          <div class="card-icon">🔬</div>
          <div class="card-text">Hardware Design</div>
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="section about-section">
    <div class="container">
      <div class="section-header reveal">
        <h2>About Me</h2>
        <p class="section-subtitle">From hardware to software, systems to research</p>
      </div>
      
      <div class="about-content reveal reveal-delay-1">
        <div class="about-intro">
          <p>Hello!</p>
          <p>➤ I'm Nathan, an ECE major at UT Austin.</p>
          <p>➤ I'm passionate about creating technology that makes a difference.</p>
          <p>➤ Below are some of the fields I have interests in:</p>
        </div>
        
        <div class="about-interests">
          <div class="interest-item">
            <span class="interest-icon">🔄</span>
            <div class="interest-content">
              <strong>Software Systems</strong> - Building OS projects; Focus: compilers, distributed systems, and research
            </div>
          </div>
          <div class="interest-item">
            <span class="interest-icon">🔧</span>
            <div class="interest-content">
              <strong>Embedded Systems</strong> - Developed HAL libraries, diagnostic tools, and firmware automation at CAT
            </div>
          </div>
          <div class="interest-item">
            <span class="interest-icon">🌐</span>
            <div class="interest-content">
              <strong>Full-Stack</strong> - Built GoodEats, a React + Supabase app recommending 50+ student food deals
            </div>
          </div>
          <div class="interest-item">
            <span class="interest-icon">🔮</span>
            <div class="interest-content">
              <strong>Quantum Computing</strong> - Strong interest, with experience implementing hybrid algorithms like VQE
            </div>
          </div>
        </div>
        
        <a href="/about" class="btn btn-secondary">Learn More About Me</a>
      </div>
    </div>
  </section>

  <!-- Featured Projects Section -->
  <section class="section featured-section">
    <div class="container">
      <div class="section-header reveal">
        <h2>Featured Projects</h2>
        <p class="section-subtitle">Some of my recent work</p>
      </div>
      
      <div class="projects-grid">
        {recentProjects.map((project, index) => (
          <div class={`project-card reveal reveal-delay-${index + 1}`}>
            <div class="card-gradient">
              <div class="card-gradient-inner">
                <div class="project-header">
                  <h3>{project.title}</h3>
                </div>
                <p class="project-date-small">{project.dateDisplay}</p>
                <p>{project.description}</p>
                <div class="project-tags">
                  {project.tags.slice(0, 3).map(tag => (
                    <span class="tag">{tag}</span>
                  ))}
                  {project.tags.length > 3 && (
                    <span class="tag">+{project.tags.length - 3}</span>
                  )}
                </div>
                <div class="project-links">
                  <a href={`/projects/${project.slug}`} class="project-link">
                    View Details →
                  </a>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      <div class="text-center reveal reveal-delay-3">
        <a href="/projects" class="btn btn-primary">View All Projects</a>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="section contact-section" id="contact">
    <div class="container">
      <div class="section-header reveal">
        <h2>Get In Touch</h2>
        <p class="section-subtitle">Let's build something amazing together!</p>
      </div>
      
      <div class="contact-content reveal reveal-delay-1">
        <ContactForm />
        
        <div class="social-links">
          <a href="https://github.com/Code-Tomato" class="social-link" target="_blank" rel="noopener noreferrer">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            GitHub
          </a>
          <a href="https://linkedin.com/in/nathanylemma/" class="social-link" target="_blank" rel="noopener noreferrer">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
            LinkedIn
          </a>
          <a href="mailto:nlemma100@gmail.com" class="social-link">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
              <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
            Email
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 2rem 0;
  }

  .hero-background {
    position: absolute;
    inset: 0;
    overflow: hidden;
    z-index: 0;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.3;
    animation: float 8s ease-in-out infinite;
    pointer-events: none;
    will-change: transform;
    transform: translateZ(0);
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
  }

  .orb-1 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--color-gradient-start) 0%, var(--color-gradient-start) 100%);
    top: -100px;
    right: -100px;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--color-gradient-mid) 0%, var(--color-gradient-mid) 100%);
    bottom: -150px;
    left: -150px;
    animation-delay: 2s;
  }

  .orb-3 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, var(--color-gradient-end) 0%, var(--color-gradient-end) 100%);
    top: 50%;
    left: 50%;
    animation-delay: 4s;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 50px;
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: #4caf50;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    margin-bottom: 1rem;
    line-height: 1.1;
  }

  .typing-container {
    min-height: 3rem;
  }

  .hero-subtitle {
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: var(--text-primary);
    margin-bottom: 1.5rem;
  }

  .typed-text {
    color: var(--color-primary);
    font-weight: 600;
  }

  .cursor {
    color: var(--color-primary);
    animation: blink 1s step-end infinite;
  }

  .hero-description {
    font-size: 1.2rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-image {
    position: relative;
    height: 400px;
  }

  .floating-card {
    position: absolute;
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-radius: 16px;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
  }

  .floating-card:hover {
    transform: translateY(-10px) !important;
    box-shadow: var(--shadow-lg);
    border-color: var(--color-primary);
  }

  .card-1 {
    top: 15%;
    left: 5%;
    animation: float 4s ease-in-out infinite;
  }

  .card-2 {
    top: 45%;
    right: 5%;
    animation: float 5s ease-in-out infinite;
    animation-delay: 1s;
  }

  .card-3 {
    bottom: 15%;
    left: 20%;
    animation: float 6s ease-in-out infinite;
    animation-delay: 2s;
  }

  .card-icon {
    font-size: 2rem;
  }

  .card-text {
    font-weight: 600;
    color: var(--text-primary);
  }

  /* Section Styles */
  .section {
    padding: 5rem 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-subtitle {
    font-size: 1.2rem;
    color: var(--text-muted);
    margin-top: -0.5rem;
  }

  .about-section {
    background: var(--bg-secondary);
  }

  .about-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: left;
  }

  .about-intro p {
    font-size: 1.2rem;
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  .about-intro p:first-child {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }


  .about-interests {
    margin: 2rem 0;
  }

  .interest-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
    padding: 1rem;
    background: var(--bg-tertiary);
    border-radius: 12px;
    border: 1px solid var(--border-color);
    transition: all 0.3s ease;
  }

  .interest-item:hover {
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-sm);
  }

  .interest-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
    margin-top: 0.2rem;
  }

  .interest-content {
    flex: 1;
    font-size: 1.1rem;
    line-height: 1.5;
  }

  .interest-content strong {
    color: var(--color-primary);
    font-weight: 600;
  }

  /* Projects Grid */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .project-card {
    display: flex;
    flex-direction: column;
  }

  .card-gradient {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .card-gradient-inner {
    display: flex;
    flex-direction: column;
    flex: 1;
    height: 100%;
  }

  .project-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .project-date-small {
    font-size: 0.9rem;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
  }

  .status-badge {
    padding: 0.25rem 0.75rem;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
  }

  .status-badge.live {
    background: rgba(76, 175, 80, 0.1);
    color: #4caf50;
  }

  .status-badge.developing {
    background: rgba(255, 152, 0, 0.1);
    color: #ff9800;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 1rem 0;
  }

  .tag {
    padding: 0.375rem 0.875rem;
    background: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    border-radius: 50px;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .project-links {
    margin-top: 1.5rem;
  }

  .project-link {
    color: var(--color-primary);
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .project-link:hover {
    color: var(--color-secondary);
  }

  .text-center {
    text-align: center;
  }

  /* Contact Section */
  .contact-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-radius: 12px;
    color: var(--text-primary);
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .social-link:hover {
    border-color: var(--color-primary);
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
  }

  /* Responsive */
  @media (max-width: 1024px) and (min-width: 769px) {
    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
      grid-auto-rows: 1fr;
    }
    
    .project-card:nth-child(3) {
      grid-column: 1 / -1;
      max-width: none;
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image {
      height: 300px;
      order: -1;
    }

    .floating-card {
      padding: 1rem;
    }

    .card-text {
      font-size: 0.9rem;
    }

    .hero-cta {
      flex-direction: column;
    }

    .hero-cta .btn {
      width: 100%;
      text-align: center;
    }

    .social-links {
      flex-direction: column;
    }

    .social-link {
      width: 100%;
      justify-content: center;
    }

    .projects-grid {
      grid-template-columns: 1fr;
      grid-auto-rows: auto;
    }

    .interest-item {
      flex-direction: row;
      text-align: left;
      gap: 1rem;
    }

    .interest-icon {
      margin-top: 0.2rem;
    }

    .about-intro p {
      font-size: 1.1rem;
    }

    .about-intro p:first-child {
      font-size: 1.3rem;
    }
  }
</style>

<script>
  // Typing animation
  const texts = [
    'operating systems',
    'embedded systems',
    'hardware designs',
    'research projects',
    'quantum algorithms',
    'system software'
  ];
  let textIndex = 0;
  let charIndex = 0;
  let isDeleting = false;
  const typedTextElement = document.querySelector('.typed-text');

  function type() {
    const currentText = texts[textIndex];
    
    if (isDeleting) {
      typedTextElement!.textContent = currentText.substring(0, charIndex - 1);
      charIndex--;
    } else {
      typedTextElement!.textContent = currentText.substring(0, charIndex + 1);
      charIndex++;
    }

    if (!isDeleting && charIndex === currentText.length) {
      isDeleting = true;
      setTimeout(type, 2000);
    } else if (isDeleting && charIndex === 0) {
      isDeleting = false;
      textIndex = (textIndex + 1) % texts.length;
      setTimeout(type, 500);
    } else {
      setTimeout(type, isDeleting ? 50 : 100);
    }
  }

  // Start typing animation
  document.addEventListener('DOMContentLoaded', () => {
    setTimeout(type, 1000);
  });
</script>